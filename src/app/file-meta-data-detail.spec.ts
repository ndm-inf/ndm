import { FileMetaDataDetail } from './file-meta-data-detail';

describe('FileMetaDataDetail', () => {
  it('should create an instance', () => {
    expect(new FileMetaDataDetail()).toBeTruthy();
  });
});
